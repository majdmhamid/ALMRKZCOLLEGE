# الـ Prompt للمحادثة القادمة — بناء المسودة الأولى للموقع

> حسين: انسخ كل اللي بين الخطّين والصقه بمحادثة جديدة بـ Claude Code.

---

نزّل المشروع من github.com/majdmhamid/ALMRKZCOLLEGE وانتقل للفرع `claude/almrkz-college-website-wzvy6c`.
الصور والفيديوهات المحمّلة من الموقع القديم موجودة على الفرع `claude/clever-heisenberg-waag9g` (مجلد `assets/`) — ادمجه بفرعنا قبل ما تبدأ. إذا مجلد `assets/images/website/` فاضي، شغّل `scripts/fetch-website-media.py` لتنزيلها.

أنا حسين، ما بعرف برمجة، احكي معي بالعربي وبلغة بسيطة، ولا تسألني أسئلة تقنية — القرارات التقنية عليك.

اقرأ بالترتيب: `CLAUDE.md` ثم `docs/PLAN.md` ثم `docs/CURRENT-SITE.md` (فيه جرد كامل للموقع الحالي: كل الدورات، النصوص، الاتصال، الألوان، وبنية الموقع الجديد المقترحة والمتفق عليها).

**المهمة:** ابنِ **المسودة الأولى** للموقع الجديد لكلية المركز للتأهيل المهني — أم الفحم. مسودة يعني: موقع كامل شغّال بكل الصفحات، بالعربي والعبري، لأعرضه على مجد (صاحب الكلية). مش لازم يكون مثالي، بس لازم يكون **أساس صحيح نقدر نزيد عليه** بدون ما نهدمه.

**التقنية المطلوبة (لا تغيّرها):**
- Next.js (App Router) + TypeScript + Tailwind CSS.
- محتوى الدورات والمجموعات والطاقم والخريجين في ملفات بيانات بسيطة داخل المشروع (مثلاً `content/courses.ts`) — بدون لوحة تحكم ولا قاعدة بيانات بهذه المرحلة، لكن بشكل يسمح بربط Payload CMS لاحقاً.
- مسارات `/ar/...` و `/he/...` مع RTL كامل، وتبديل لغة يودّي لنفس الصفحة باللغة الثانية.
- خط Almarai للعربي، الألوان من `docs/CURRENT-SITE.md` (أخضر `#158942` / `#88bb3c`، رمادي `#333333`).
- استخدم اللوغو والصور الحقيقية الموجودة في `assets/images/website/` (اضغطها وصغّرها قبل الاستخدام، الصور الحالية ثقيلة). لا تستخدم صور جاهزة من الإنترنت.
- استمارة "سجّل اهتمامك": اسم + هاتف (إلزامي) + الدورة، الإيميل اختياري. بهذه المرحلة تكفي إنها ترسل لإيميل الكلية (`almerkaz.collega@gmail.com`) أو تطبع بالسجل، مع ملاحظة واضحة وين نعبّي مفاتيح الإرسال لاحقاً.
- زر واتساب عائم بكل الصفحات: `https://wa.me/972546174339`.
- SEO أساسي: عنوان ووصف لكل صفحة باللغتين، hreflang، sitemap، بيانات Schema من نوع EducationalOrganization.
- أداة الوصولية (נגישות): ضع مكانها وتعليق يشرح كيف تُركّب لاحقاً.

**الصفحات المطلوبة (كلها بالعربي والعبري):**
الرئيسية · الدورات (٣ مجموعات: الحديد/لحام، التكييف والتبريد، البناء والسلامة) · صفحة لكل دورة من الـ ١١ · عن الكلية · الخريجون · معرض الصور والفيديو · أخبار · للشركات والمشغّلين · اتصل بنا.
بنية صفحة الدورة موجودة بالتفصيل في `docs/CURRENT-SITE.md` قسم 6.

**المحتوى:**
- انقل نصوص الدورات من الموقع الحالي (النصوص الخام محفوظة في `docs/current-site-raw/`)، وأعد كتابتها بعربي مفهوم وعبري سليم (ليس ترجمة آلية). الدورات اللي وصفها عبري فقط اكتب لها وصف عربي.
- صحّح الأخطاء الإملائية المذكورة بالتقرير.
- التواريخ القديمة لا تُعرض؛ بدلها: "الموعد القادم: تواصل معنا".
- مجموعة "إدارة وتسويق" لا تُعرض.

**قواعد ممنوع تخالفها (من CLAUDE.md):**
1. لا أسعار بالموقع أبداً.
2. لا أي صياغة توحي بضمان تشغيل. المسموح: "مرافقة وتوجيه مهني بعد التخرّج" + التنويه: «الكلية تقدّم مرافقة وتوجيه مهني، ولا تلتزم بتأمين مكان عمل.»
3. المنح: "الدورة ملائمة للحصول على منحة — تواصل معنا للاستشارة" فقط.

**لما تخلص:**
1. شغّل الموقع محلياً وافتحه لي بالمتصفح عشان أشوفه (كمبيوتر وموبايل).
2. ارفع الشغل على GitHub على نفس الفرع.
3. اكتب لي بالعربي: شو خلص، شو ناقص، وشو لازم أجيب من مجد.
4. حدّث `docs/HANDOFF.md` عشان المحادثة اللي بعدك تعرف وين وصلنا.

---
